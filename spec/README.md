This folder contains PickRank product and implementation specifications.

- `product_spec.md` is the master product spec.
- `features/` contains the canonical feature, system, QA, and implementation specs.

Older empty placeholder files and the legacy singular `feature/` draft folder were moved to the PickRank Obsidian archive so this repo only carries active source-of-truth markdown.
